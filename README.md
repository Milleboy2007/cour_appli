# cour_appli
# *npm create vite@latest*

Commande de base
git add .
git commit -m ""
git push

git pull (push mais de git au pc)
git branch
git checkout name (for switch branch)


CRUD:
    Create -> post()
    Read -> get()
    Update -> 
    Delete -> 


